# Contributing

변경 전 이슈에서 범위와 사용자 동작을 합의하세요.

## 제출 기준

- Visual Studio 2022 x64 Release 빌드 및 `--self-test-core` 통과
- 패키징 변경 시 `--self-test-tools` 통과
- 사용자 입력을 명령행에 전달할 때 `CommandLine_Build` 사용
- 작업 중 변경되면 안 되는 설정을 시작 시점에 복사
- 외부 실행 파일의 버전·SHA-256·라이선스 동시 갱신
- 안전한 경로 처리와 일반 사용자가 이해할 수 있는 오류 문구 유지

수동 검사는 다운로드 권한이 있는 콘텐츠로만 수행하세요.
