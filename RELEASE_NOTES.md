# v1.1.4

- 로딩창과 프로그램 정보 화면을 Febius 디자인으로 개편
- Febius 공식 심볼과 Downrush 제품 아이콘 적용

# v1.1.3

- 버그 수정

# v1.1.2

- 최신 YouTube 추출에 필요한 Deno JavaScript 런타임을 내장하고 `yt-dlp` 실행 환경 보강
- 항목별 최종 MP3 파일명 직접 편집·고정 및 ID3 메타데이터 삽입 지원
- 영상 ID·음질·실제 파일명을 기록하는 다운로드 이력 v2와 구버전 기록 자동 이전
- 취소·시간 제한·하위 프로세스 일괄 종료, 고유 임시 폴더와 비정상 종료 잔여 파일 정리 추가
- 단일 인스턴스, 순환 진단 로그, 기존 Seowol 설정·도구 폴더 자동 이전 지원
- 내장 도구 경로 제한, 의존성 버전·SHA-256 고정, 정확한 제3자 라이선스 고지로 배포 안전성 강화
- 시스템 DPI 크기 조정, 키보드 Tab 이동, 전용 앱 아이콘과 화면 잠금 상태 개선
- Windows 자체 검사, 단일 EXE 도구 실행 검사, 주간 의존성·YouTube 호환성 점검 워크플로 추가

# v1.1.1

- 옵션 메뉴에서 MP3 음질 선택 기능 추가 (`128`, `192`, `256`, `320 kbps`)

# v1.1.0

- 가벼운 전용 시작 화면과 단계별 준비 상태 표시 추가
- 앱 내 최신 버전 확인 및 릴리스 정보 메뉴 추가

# v1.0.5

- 정식 제품 정보, 저작권 및 라이선스 화면 추가
- 프로그램 창을 먼저 표시하고 다운로드 구성 요소를 뒤에서 준비하도록 시작 과정 개선
- 작업 목록 메모리 사용, 화면 갱신, 작업자 생성 및 기록 조회 최적화
- 실행 파일 버전 정보와 제3자 소프트웨어 고지 보강

# v1.0.4

- `(공식 뮤비)` 등 공식 영상 표기와 빈 괄호를 함께 제거하고 `Live`, `Acoustic`, `feat.` 표기는 유지
- 메타데이터 조회를 최대 4개, 다운로드를 최대 2개까지 병렬 처리
- 도구 경로와 다운로드 기록을 반복 탐색하지 않도록 캐시해 대기 시간 단축
- 목록의 진행률 열을 없애고 항목별 진행 상황을 상태 열에 통합
- Windows 95/2000풍 클래식 메뉴, 상태 표시줄, 각진 전체 진행 표시 적용
- 파일명 처리와 명령행 생성을 독립 모듈로 분리
- URL과 저장 경로를 안전한 Windows 명령행 인자로 전달하도록 수정
- 사용자 전역 `yt-dlp` 설정으로 다운로드 동작이 달라지는 문제 방지
- 경로 이탈이 가능한 파일명과 너무 긴 저장 경로 차단
- UTF-16 BE 링크 목록 불러오기 지원
- 정보 조회·다운로드 완료 시 성공/건너뜀/실패 수와 선택한 항목의 오류 표시
- 핵심 문자열 처리 자체 검사 및 CI 실행 추가
- 기존 버전의 릴리스 파일을 실수로 덮어쓰지 않도록 배포 절차 수정

# v1.0.2

- 링크 추가 후 정보 조회가 끝날 때 프로그램이 종료되던 문제 수정
- 중복 제거 버튼 사용 시 프로그램이 종료되던 문제 수정
- 기본 저장 폴더를 `Music\\YouTubeMP3`로 변경
- `yt-dlp`, `ffmpeg`, `ffprobe`를 EXE에 포함해 별도 설치 없이 실행 가능
