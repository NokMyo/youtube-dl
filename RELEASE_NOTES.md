# 다음 버전

- URL과 저장 경로를 안전한 Windows 명령행 인자로 전달하도록 수정
- 사용자 전역 `yt-dlp` 설정으로 다운로드 동작이 달라지는 문제 방지
- 경로 이탈이 가능한 파일명과 너무 긴 저장 경로 차단
- UTF-16 BE 링크 목록 불러오기 지원
- 정보 조회·다운로드 완료 시 성공/건너뜀/실패 수와 선택한 항목의 오류 표시
- 핵심 문자열 처리 자체 검사 및 CI 실행 추가
- 기존 버전의 릴리스 파일을 실수로 덮어쓰지 않도록 배포 절차 수정

# v1.0.2

- 링크 추가 후 정보 조회가 끝날 때 프로그램이 종료되던 문제 수정
- 중복 제거 버튼 사용 시 프로그램이 종료되던 문제 수정
- 기본 저장 폴더를 `Music\\YouTubeMP3`로 변경
- `yt-dlp`, `ffmpeg`, `ffprobe`를 EXE에 포함해 별도 설치 없이 실행 가능
